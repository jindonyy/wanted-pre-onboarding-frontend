import { color, palette } from '@/styles/theme/color';
import { font } from '@/styles/theme/font';
import { size } from '@/styles/theme/size';
import { mixins } from '@/styles/theme/mixins';

export const theme = { color, palette, font, size, mixins };
